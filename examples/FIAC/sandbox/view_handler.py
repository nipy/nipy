#-------------------------------------------------------------------------------
#
#  The 'ViewHandler' traits view definition and handler.
#
#-------------------------------------------------------------------------------
#
#  Generated by the VET tool (version 0.1.0) on Sat May 27 21:09:17 2006.
#
#-------------------------------------------------------------------------------
#
#  Usage is:
#    ViewHandler().edit_traits( context = object_to_edit, ... )
#
#-------------------------------------------------------------------------------

#-------------------------------------------------------------------------------
#  Imports:
#-------------------------------------------------------------------------------

from enthought.traits.ui      import Handler, View, Group, Item
from enthought.traits.ui.menu import MenuBar, Menu, Action, Separator

#-------------------------------------------------------------------------------
#  'ViewHandler' class:
#-------------------------------------------------------------------------------

class ViewHandler ( Handler ):
    
    #---------------------------------------------------------------------------
    #  Traits view definition:    
    #---------------------------------------------------------------------------
        
    traits_view = View(
        Group(
            Item(
                name   = 'run',
                object = 'RunUI'
            ),
            Item(
                name    = 'subj',
                object  = 'RunUI',
                style   = 'readonly',
                tooltip = u'which?'
            ),
            Item(
                name   = 'mask',
                object = 'RunUI'
            )
        ),
        Group(
            Item(
                name   = 'drift_df',
                object = 'RunUI'
            ),
            Item(
                name   = 'knots',
                object = 'RunUI'
            ),
            Item(
                name   = 'normalize',
                object = 'RunUI'
            ),
            Item(
                name   = 'norm_reg',
                object = 'RunUI'
            )
        )
    ) 
    
#-------------------------------------------------------------------------------
#  'ViewHandler' test case: 
#-------------------------------------------------------------------------------

if __name__ == '__main__':
    import ui
    ViewHandler().configure_traits( context = {
        'RunUI': ui.RunUI()
    } )
